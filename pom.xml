<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.acme</groupId>
  <artifactId>llm-tool-agent</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <name>Agentic OpenWorkflow Reference Implementation</name>
  <description>Production-ready reference implementation for extending the OpenWorkflow specification with native Agentic AI capabilities</description>

  <properties>
    <quarkus-plugin.version>3.27.5.1</quarkus-plugin.version>
    <quarkus.platform.group-id>io.quarkus</quarkus.platform.group-id>
    <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
    <quarkus.platform.version>3.27.5.1</quarkus.platform.version>
    <kogito.bom.version>10.2.0</kogito.bom.version>
    <sonataflow.version>10.2.0</sonataflow.version>
    <!-- Security floors: Kogito 10.2 pins us to the Quarkus 3.27 line, whose managed
         Netty (4.1.130) and Jackson (2.19.2) are below the fixed versions for several
         HIGH CVEs. The BOM imports below are declared BEFORE quarkus-bom so they win
         Maven's first-import-wins conflict resolution and lift the whole artifact
         families to the exact advisory floors (netty >= 4.1.136: CVE-2026-56819 and
         others; jackson >= 2.21.5: CVE-2026-54512/54513, GHSA-r7wm-3cxj-wff9,
         CVE-2026-54515, CVE-2026-59889, GHSA-mhm7-754m-9p8w). -->
    <netty.bom.version>4.1.136.Final</netty.bom.version>
    <jackson.bom.version>2.21.5</jackson.bom.version>
    <maven.compiler.release>17</maven.compiler.release>
    <version.compiler.plugin>3.8.1</version.compiler.plugin>
    <version.surefire.plugin>3.5.5</version.surefire.plugin>
    <version.resources.plugin>3.4.0</version.resources.plugin>
    <version.antrun.plugin>3.1.0</version.antrun.plugin>
    <studio.frontend.plugin.version>1.15.4</studio.frontend.plugin.version>
    <studio.node.version>v24.18.0</studio.node.version>
    <studio.npm.version>11.6.2</studio.npm.version>
    <studio.validation.rule-set>studio-validation-2026-08-20.1</studio.validation.rule-set>
    <studio.validation.workflow-profile>workflow-0.8-local-1</studio.validation.workflow-profile>
    <studio.validation.catalog-profile>openapi-3.x-local-1</studio.validation.catalog-profile>
    <studio.skip>false</studio.skip>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Declared first: these security-floor BOMs must override the versions managed
           by quarkus-bom below (first import wins for the artifacts they both manage). -->
      <dependency>
        <groupId>io.netty</groupId>
        <artifactId>netty-bom</artifactId>
        <version>${netty.bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>${quarkus.platform.artifact-id}</artifactId>
        <version>${quarkus.platform.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.kie.kogito</groupId>
        <artifactId>kogito-bom</artifactId>
        <version>${kogito.bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Rhino 1.7.7.2 (transitive via swagger-parser -> json-schema-validator ->
           json-schema-core, used for JS schema validation) has CVE-2025-66453 (LOW).
           1.8.1 is API-compatible; override the Kogito-managed version. -->
      <dependency>
        <groupId>org.mozilla</groupId>
        <artifactId>rhino</artifactId>
        <version>1.9.1</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.apache.kie.sonataflow</groupId>
      <artifactId>sonataflow-quarkus</artifactId>
      <version>${sonataflow.version}</version>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-openapi</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-smallrye-health</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-resteasy-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-yaml</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-arc</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-reactive-routes</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>${project.artifactId}</finalName>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${version.compiler.plugin}</version>
        <configuration><release>${maven.compiler.release}</release></configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>${version.antrun.plugin}</version>
        <executions>
          <execution>
            <id>clean-studio-classpath-output</id>
            <phase>process-resources</phase>
            <goals><goal>run</goal></goals>
            <configuration>
              <skip>${studio.skip}</skip>
              <target>
                <delete dir="${project.build.outputDirectory}/META-INF/resources/studio" />
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>${version.resources.plugin}</version>
        <executions>
          <execution>
            <id>copy-resources-before-codegen</id>
            <phase>generate-sources</phase>
            <goals><goal>resources</goal></goals>
          </execution>
          <execution>
            <id>copy-studio-dist</id>
            <phase>process-resources</phase>
            <goals><goal>copy-resources</goal></goals>
            <configuration>
              <skip>${studio.skip}</skip>
              <resources>
                <resource>
                  <directory>${project.build.directory}/studio-dist</directory>
                  <filtering>false</filtering>
                </resource>
              </resources>
              <outputDirectory>${project.build.outputDirectory}/META-INF/resources/studio</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${studio.frontend.plugin.version}</version>
        <configuration>
          <workingDirectory>${project.basedir}/frontend/studio</workingDirectory>
          <installDirectory>${project.build.directory}/studio-node</installDirectory>
          <skip>${studio.skip}</skip>
        </configuration>
        <executions>
          <execution>
            <id>studio-install-node-and-npm</id>
            <phase>generate-resources</phase>
            <goals><goal>install-node-and-npm</goal></goals>
            <configuration>
              <nodeVersion>${studio.node.version}</nodeVersion>
              <npmVersion>${studio.npm.version}</npmVersion>
            </configuration>
          </execution>
          <execution>
            <id>studio-npm-ci</id>
            <phase>generate-resources</phase>
            <goals><goal>npm</goal></goals>
            <configuration><arguments>ci</arguments></configuration>
          </execution>
          <execution>
            <id>studio-lint</id>
            <phase>generate-resources</phase>
            <goals><goal>npm</goal></goals>
            <configuration><arguments>run lint</arguments></configuration>
          </execution>
          <execution>
            <id>studio-typecheck</id>
            <phase>generate-resources</phase>
            <goals><goal>npm</goal></goals>
            <configuration><arguments>run typecheck</arguments></configuration>
          </execution>
          <execution>
            <id>studio-unit-tests</id>
            <phase>generate-resources</phase>
            <goals><goal>npm</goal></goals>
            <configuration><arguments>run test:unit -- --run</arguments></configuration>
          </execution>
          <execution>
            <id>studio-build</id>
            <phase>generate-resources</phase>
            <goals><goal>npm</goal></goals>
            <configuration><arguments>run build</arguments></configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>${quarkus.platform.group-id}</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${quarkus-plugin.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals><goal>build</goal><goal>generate-code</goal><goal>generate-code-tests</goal></goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${version.surefire.plugin}</version>
        <configuration>
          <argLine>--add-opens java.base/java.lang=ALL-UNNAMED</argLine>
          <systemPropertyVariables>
            <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
